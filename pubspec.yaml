name: flutter_aplicando_persistencia_de_dados
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.18.6 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    cupertino_icons: ^1.0.2
    sqflite: ^2.2.3
    path: ^1.8.2

dev_dependencies:
    dart_code_metrics: ^5.6.0
    flutter_test:
        sdk: flutter

    flutterando_analysis: ^0.0.1

flutter:
    assets:
        - assets/images/
    uses-material-design: true
